package com.wasim.attendancemanager.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette (calm indigo/blue) ───────────────────────────────────────
val AppPrimary        = Color(0xFF3B5BDB)   // indigo-600
val AppPrimaryLight   = Color(0xFF748FFC)   // indigo-400
val AppPrimaryDark    = Color(0xFF1E3A8A)   // indigo-900
val AppOnPrimary      = Color(0xFFFFFFFF)

// ── Surface / Background ──────────────────────────────────────────────────────
val AppBackground     = Color(0xFFF4F6FB)   // very light blue-grey
val AppSurface        = Color(0xFFFFFFFF)
val AppSurfaceVariant = Color(0xFFEEF1F8)

// ── Status colors ─────────────────────────────────────────────────────────────
val AppPresent        = Color(0xFF12B886)   // teal-green
val AppAbsent         = Color(0xFFFA5252)   // red
val AppCheckedOut     = Color(0xFFF59F00)   // amber
val AppInactive       = Color(0xFF868E96)   // grey

// ── Text ──────────────────────────────────────────────────────────────────────
val AppTextPrimary    = Color(0xFF1A1D2E)
val AppTextSecondary  = Color(0xFF6B7280)
val AppDivider        = Color(0xFFE5E7EB)

// ── Card shadow accent ────────────────────────────────────────────────────────
val AppCardShadow     = Color(0x1A3B5BDB)

