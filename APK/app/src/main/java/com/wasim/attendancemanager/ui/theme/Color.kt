package com.wasim.attendancemanager.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette ───────────────────────────────────────────────────────────
val AppPrimary        = Color(0xFF3B5BDB)
val AppPrimaryLight   = Color(0xFF748FFC)
val AppPrimaryDark    = Color(0xFF1E3A8A)
val AppOnPrimary      = Color(0xFFFFFFFF)

// ── Light theme — soft off-white background, pure white cards ────────────────
val AppBackground     = Color(0xFFF2F4F8)   // soft cool off-white — cards pop against this
val AppSurface        = Color(0xFFFFFFFF)   // pure white cards
val AppSurfaceVariant = Color(0xFFEBEDF2)   // slightly deeper for inputs/variants

// ── Dark theme — deep navy/charcoal with vibrant accents ─────────────────────
val DarkBackground    = Color(0xFF0D0F18)   // near-black with blue undertone
val DarkSurface       = Color(0xFF151825)   // deep navy card
val DarkSurfaceVariant= Color(0xFF1E2235)   // slightly lighter card
val DarkPrimary       = Color(0xFF7B9EFF)   // bright indigo for dark bg
val DarkOnPrimary     = Color(0xFF0D0F18)

// ── Status colors ─────────────────────────────────────────────────────────────
val AppPresent        = Color(0xFF12B886)
val AppAbsent         = Color(0xFFFA5252)
val AppCheckedOut     = Color(0xFFF59F00)
val AppInactive       = Color(0xFF868E96)

// ── Text ──────────────────────────────────────────────────────────────────────
val AppTextPrimary    = Color(0xFF111827)   // near-black
val AppTextSecondary  = Color(0xFF6B7280)
val AppDivider        = Color(0xFFE5E7EB)

// ── 3-D shadow accent ─────────────────────────────────────────────────────────
val AppCardShadow     = Color(0x1A3B5BDB)
